#include <stdio.h>
int main() {
    int n, i, suma = 0;
    printf("Ingrese N: ");
    scanf("%d", &n);
    for(i = 1; i <= n; i++) {
        suma += i;
    }
    printf("Suma = %d\n", suma);
    return 0;
}
